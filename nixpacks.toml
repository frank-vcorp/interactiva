[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]
aptPkgs = ["poppler-utils", "tesseract-ocr", "tesseract-ocr-spa"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
