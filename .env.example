# Database (Coolify PostgreSQL in staging)
DATABASE_URL=postgresql://user:password@localhost:5432/interactiva

# Session / JWT
SESSION_SECRET=change-me-to-a-long-random-string-min-32-chars

# Cookie security (true in production/staging HTTPS)
COOKIE_SECURE=false

# Superuser seed (only used on first bootstrap)
INTERACTIVA_SUPERUSER_PASSWORD=VectorIA2026@

# App
NODE_ENV=development
PORT=3000
